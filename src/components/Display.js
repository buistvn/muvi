/*
import React from 'react';
import './Display.css';

function Display(pageType) {
    var headerName = "";

    if (pageType == "popular") {
        headerName = "Popular Films";
    }
    else if (pageType == "search") {
        headerName = "Results";
    }

    return (
        <div className="displayContainer">
            <h1 className="displayHeader">{headerName}</h1>
        </div>
    );
}

export default Display;
*/